<!-- SPDX-FileCopyrightText: 2026 Robert Pelfrey <Robert@Pelfrey.de> -->
<!-- SPDX-License-Identifier: GPL-3.0-or-later -->

# GMP/XML Strangler Map

TurboVAS inherits GMP/XML as the main manager protocol. GMP remains useful for
compatibility and high-consequence scanner control, but it should no longer be
the default shape for new TurboVAS product data flow. Native HTTP/JSON product
APIs should replace inherited client/tooling paths workflow by workflow.

Explicit end-state: TurboVAS should remove the need for `python-gvm` and
`gvm-tools` entirely from required product, runtime-helper, test, and operator
tooling paths. They may remain temporarily as compatibility bridges while
replacement APIs land.

The end-state also includes scriptable access to TurboVAS through typed
HTTP/JSON and OpenAPI clients. The authenticated same-origin `gsad` proxy is a
browser migration bridge, not the final API boundary and not a new reason to
keep `gsad` in every automation path. Direct scriptable API work is now active:
the first slice is an opt-in read-only bearer-auth development listener. TLS,
production host binding, audit, rate limits, and write-safety controls remain
separate hardening work before production exposure.

The first live proof is the Rust `turbovas-api` sidecar for
raw report reads, scope-report collections, target/task reads, scanner metadata,
saved filters, tags, scan configs, overrides, port lists, schedules, report formats, and report metrics, currently raw report list/detail/result rows/hosts,
target/task internal reads, the browser-backed target and task lists, top-level
Results, Vulnerabilities, Operating Systems, Hosts, TLS Certificates, and
Scanners, Scan Configs, Filters, Tags, Overrides, Port Lists, and Schedules lists, scope-report list, Results, Hosts, Ports, Applications,
Operating Systems, CVEs, TLS Certificates, Error Messages, scope-report Metrics,
and raw report Metrics. It queries PostgreSQL
directly. It is internal by default and has an opt-in bearer-auth direct
development listener for read-only scriptable access. Browser migration now covers the raw `/reports` list,
the `/targets` list, the `/tasks` list, raw-report Results/Hosts, raw-report
and scope-report Metrics, plus all current scope-report evidence tabs through
the authenticated same-origin `gsad` proxy defined in
`docs/NATIVE_API_AUTH_BOUNDARY.md`.

## Workflow Retirement Classes

| Class | Meaning | Current candidates |
| --- | --- | --- |
| Ready for native API proof | Low-risk read workflows with DB-owned state and existing validation. | Raw report reads, scope metadata reads, and remaining helper/tooling replacement paths. |
| Later after product semantics | Workflows needing stronger operator-model decisions first. | Exposure-duration views, owner/patchability/support status, non-operator delivery, BYO inventory. |
| High-consequence control path | Keep inherited control path until a separate design proves safety. | Scan start/stop, credential secret/control paths, account/auth, feed import, scanner registration, runtime feed state. |
| Compatibility-only | Retain only until no required TurboVAS workflow depends on it. | `python-gvm` request helpers, `gvm-tools` GMP scripts, direct XML report helper paths. |

## Initial Map

| Workflow | Current path | Native target | Retirement criterion |
| --- | --- | --- | --- |
| Raw result/report list/detail/evidence tabs | GSA GMP commands -> gsad -> gvmd XML -> PostgreSQL | `/api/v1/results`, `/api/v1/results/{result_id}`, `/api/v1/reports`, `/api/v1/reports/{report_id}`, `/api/v1/reports/{report_id}/results`, `/hosts`, `/ports`, `/applications`, `/operating-systems`, `/cves`, `/tls-certificates`, and `/errors` | `/results` and `/reports` now read their lists through typed JSON. Raw result detail now tries `/api/v1/results/{result_id}` first for bounded metadata, result/NVT explanatory fields, NVT-level EPSS context, active result user tags, result CVE/CERT/xref references, and effective active override display metadata; inherited GMP/XML remains the fallback for result tag mutation, exports, actions, create-override, override mutation, and still-unmigrated rich detail surfaces. Raw detail summary plus raw report Results, Hosts, Ports, Applications, Operating Systems, CVEs, TLS Certificates, and Error Messages tab reads use native JSON through the authenticated `gsad` proxy. The inherited Closed CVEs report view was removed instead of migrated. |
| Security Information CVE catalog | GSA GMP commands -> gsad -> gvmd XML -> SCAP/PostgreSQL | `/api/v1/cves` and `/api/v1/cves/{cve_id}` | The GSA `/cves` list and `/cve/{id}` detail now read the retained CVE intelligence catalog through native JSON, including EPSS score/percentile, generic URL/tag references, SCAP configuration nodes, and CERT/NVT references where PostgreSQL stores them. EPSS provenance remains out of scope until SCAP/feed schema records a real source. This is intentionally separate from observed report/scope CVEs and `/vulnerabilities`. |
| Security Information CPE catalog | GSA GMP commands -> gsad -> gvmd XML -> SCAP/PostgreSQL | `/api/v1/cpes` and `/api/v1/cpes/{cpe_id}` | The GSA `/cpes` list and `/cpe/{id}` detail now read the retained CPE intelligence catalog through native JSON. This is intentionally separate from observed report/scope application and operating-system evidence. |
| Security Information NVT catalog metadata | GSA GMP list/detail commands -> gsad -> gvmd XML -> NVT PostgreSQL tables; preferences/export/config expansion still use inherited paths | `/api/v1/nvts` and `/api/v1/nvts/{nvt_id}` catalog metadata | The GSA `/nvts` list reads native JSON through the authenticated `gsad` proxy. NVT detail Information fields now overlay read-only native JSON while inherited GMP context still owns preferences, User Tags, override creation/list context, export, selector/config expansion, feed-control, scanner-control, and write semantics. The native detail uses strict numeric dotted NVT OIDs, reads `nvts` plus `vt_refs`, and maps summary/insight/affected/impact/detection into the existing GSA tag-backed Information view. |
| Security Information CERT-Bund and DFN-CERT advisory catalog metadata | GSA GMP list commands -> gsad -> gvmd XML -> CERT PostgreSQL tables; rich details still use feed XML/XSLT | `/api/v1/cert-bund-advisories`, `/api/v1/cert-bund-advisories/{advisory_id}`, `/api/v1/dfn-cert-advisories`, and `/api/v1/dfn-cert-advisories/{advisory_id}` | The GSA advisory lists already read native JSON through the authenticated `gsad` proxy. The new detail endpoints are internal catalog-detail metadata for automation and smoke coverage only; retained rich GSA detail/export behavior remains inherited because PostgreSQL does not store XML-only fields such as CERT-Bund revision history, platform/risk/source links, or DFN extra links. |
| CERT-Bund report helper | Legacy `components/gvm-tools/scripts/certbund-report.gmp.py` parsed GMP/XML report data and used a script-private CSV helper. | `tools/turbovasctl runtime-certbund-report` / `just runtime-certbund-report` backed by native report result/host and CERT-Bund advisory reads. | The native helper preserves task/report selection plus JSON/CSV output for the runtime/operator workflow, so the inherited GMP script and private `ssv_csv.py` helper are removed. This deliberately drops arbitrary `gvm-script`/GMP remote compatibility for that helper; inherited report-format exports remain separate. |
| Raw report metrics | `runtime-report-metrics` and the GSA Metrics tab now use the native API; the inherited `get_report_metrics` GMP command remains available during transition | `/api/v1/reports/{report_id}/metrics` | Runtime and browser smoke continue to prove the native path while GMP compatibility remains intact. |
| Scope list/detail and direct metadata/membership writes | GMP scope commands and GSA scope pages | `/api/v1/scopes` and `/api/v1/scopes/{scope_id}` | Scope metadata and membership reads move to typed JSON. Direct bearer-token scope create/update/delete routes are available only when direct write-control is enabled with a verified operator; browser writes remain inherited until separately migrated. |
| Scope-report list/detail | GMP scope-report commands and GSA scope-report pages | `/api/v1/scope-reports` and canonical scoped detail path | GSA list/detail reads use server-backed JSON collections and browser smoke remains green. |
| Target reads and direct metadata patch | GSA/GMP target commands -> gsad -> gvmd XML -> PostgreSQL | `/api/v1/targets`, `/api/v1/targets/{target_id}`, and direct `PATCH /api/v1/targets/{target_id}` when write-control is enabled. | `/targets` and target detail Information reads now use typed JSON through the authenticated `gsad` proxy, including safe credential references already visible in the UI. Direct bearer-token PATCH can update target name/comment metadata only with verified operator identity and `TURBOVAS_API_DIRECT_WRITE_CONTROL`; hosts, exclude hosts, port-list references, alive-test behavior, reverse-DNS flags, simultaneous-IP behavior, credential links, clone, export, delete, creation, and task/control semantics remain inherited until separately designed. Secret credential material is never exposed. |
| Task reads and metadata export | GSA/GMP task commands -> gsad -> gvmd XML -> PostgreSQL | `/api/v1/tasks`, `/api/v1/tasks/{task_id}`, and direct `/api/v1/tasks/{task_id}/export` metadata JSON | `/tasks` and task detail Information reads now use typed JSON through the authenticated `gsad` proxy, including task status, progress, trend, scanner type, references, report counts, latest report metadata, severity, and timestamps. Direct scriptable task metadata export reuses the same redacted task detail JSON for operator automation. Task writes, clone, file export, start/stop, and scanner-control semantics remain inherited until separately designed. |
| Scanner metadata reads and export | GSA/GMP scanner list/detail commands -> gsad -> gvmd XML -> PostgreSQL | `/api/v1/scanners`, `/api/v1/scanners/{scanner_id}`, and direct `/api/v1/scanners/{scanner_id}/export` metadata JSON | `/scanners` reads its list through typed JSON, exposing only scanner metadata and safe credential references. Scanner detail now includes native Information fields, active User Tags, and non-hidden task backlinks for safe socket/builtin page-load reads. Direct scriptable scanner metadata export reuses that redacted detail JSON for automation, while inherited GMP remains the fallback for remote scanner certificate context and still owns verify, file export/download, credential/certificate download context, delete, clone, edit, and scanner-control behavior. |
| Scan-config metadata reads | GSA/GMP scan-config list/detail commands -> gsad -> gvmd XML -> PostgreSQL | `/api/v1/scan-configs` and `/api/v1/scan-configs/{scan_config_id}` metadata | `/scan-configs` now reads its list through typed JSON through the authenticated `gsad` proxy. Scan Config detail Information fields, active User Tags, and shallow non-hidden task backlinks now overlay read-only native JSON while inherited GMP context still owns scanner/NVT preferences, selector/family expansion, scanner reference context, import/export, edit/delete actions, and writes until native resource/write semantics are designed. The native payload keeps family/NVT counts, growth flags, predefined/deprecated state, in-use state, owner, comments, and timestamps. |
| Saved filter reads | GSA/GMP filter list/detail commands -> gsad -> gvmd XML -> PostgreSQL | `/api/v1/filters` and `/api/v1/filters/{filter_id}` | `/filters` and filter detail reads now use typed JSON through the authenticated `gsad` proxy. Filter writes and export/import actions remain inherited until native write semantics are designed. Filter terms remain authenticated operator data, not public catalog data. |
| Tag metadata, assigned-resource, and resource-name reads | GSA/GMP tag list/detail commands plus per-resource `tag_id` list expansion and `get_resource_names` for supported tag picker types -> gsad -> gvmd XML -> PostgreSQL | `/api/v1/tags`, `/api/v1/tags/{tag_id}`, `/api/v1/tags/{tag_id}/resources`, and `/api/v1/tags/resource-names/{resource_type}` | `/tags`, tag detail metadata, the tag detail Assigned Items tab, and the Tag dialog resource-name picker now use typed JSON through the authenticated `gsad` proxy for supported resource types including redacted Alert id/name lookup. Assigned resources and resource names are read-only strict-whitelist references with only id/type/name for existing detail links or picker labels. Browser tag create, modify, clone, enable/disable, export, delete, unsupported resource types, and write semantics remain inherited until browser-native write semantics are designed. |
| Direct tag metadata and active-resource writes | Inherited GMP `create_tag`/`modify_tag` paths remain the browser compatibility path; filter-based tag assignment, clone/export/trash behavior remains inherited. | Direct `POST /api/v1/tags`, `PATCH /api/v1/tags/{tag_id}`, unassigned-tag `DELETE /api/v1/tags/{tag_id}`, and `POST /api/v1/tags/{tag_id}/resources` when direct write-control is enabled. | Direct bearer-token tag metadata create/update/delete and explicit add/remove assignment for supported native-safe active-table resource types are available only with verified operator identity and `TURBOVAS_API_DIRECT_WRITE_CONTROL`. The native delete slice is still unassigned-tag only; filter/bulk assignment, set/replace semantics, resource-type patching, clone/copy, export, trash, credentials, users, reports, and results remain inherited. |
| Credential metadata reads and direct metadata patch | GSA/GMP credential list/detail commands -> gsad -> gvmd XML -> PostgreSQL; credential secret/control writes remain inherited. | `/api/v1/credentials`, `/api/v1/credentials/{credential_id}`, and direct `PATCH /api/v1/credentials/{credential_id}` when write-control is enabled. | Native reads expose redacted credential metadata. Direct bearer-token PATCH can update name/comment metadata only with verified operator identity and `TURBOVAS_API_DIRECT_WRITE_CONTROL`; secret material, credential-store selectors, type/allow-insecure settings, scanner/target links, export/download, create/clone/restore/delete, and secret-bearing writes remain inherited. |
| Override metadata reads | GSA/GMP override list/detail commands -> gsad -> gvmd XML -> PostgreSQL | `/api/v1/overrides` and `/api/v1/overrides/{override_id}` | `/overrides` and override detail metadata reads now use typed JSON through the authenticated `gsad` proxy. The native payload keeps NVT identity, text, host/port constraints, severity override values, active state, and shallow task/result links while create, modify, clone, export, delete, trashcan mutation, and result-specific expansion remain inherited until native write/control semantics are designed. |
| Port-list reads and write-control | GSA/GMP port-list list/detail/import/write commands -> gsad -> gvmd XML -> PostgreSQL | `/api/v1/port-lists`, `/api/v1/port-lists/{port_list_id}`, `/api/v1/port-list-imports`, clone/restore/trash endpoints, and native range-set write endpoints | `/port-lists`, port-list detail, range add/delete, typed create, metadata/range update, clone, trash, restore, hard-delete, and exported-XML import now use typed JSON through the authenticated `gsad` proxy or direct write-control when enabled. Native import accepts one exported XML payload with explicit TCP/UDP ranges and preserves the imported UUID. Implicit default-range imports and bulk/file export remain inherited. |
| Schedule reads | GSA/GMP schedule list/detail commands -> gsad -> gvmd XML -> PostgreSQL | `/api/v1/schedules` and `/api/v1/schedules/{schedule_id}` | `/schedules` and schedule detail reads now use typed JSON through the authenticated `gsad` proxy. The native payload keeps iCalendar recurrence data and task backlinks while schedule writes, clone, export, and delete remain inherited until native write semantics are designed. |
| Report-config reads | GSA/GMP report-config list/detail commands -> gsad -> gvmd XML -> PostgreSQL | `/api/v1/report-configs` and `/api/v1/report-configs/{report_config_id}` | `/reportconfigs` and report-config detail reads now use typed JSON through the authenticated `gsad` proxy. The native payload keeps owner metadata, report-format references, alert backlinks, and resolved parameter metadata while report-config writes, clone, export, and delete remain inherited until native write semantics are designed. |
| Report-format reads and metadata export | GSA/GMP report-format list/detail commands -> gsad -> gvmd XML -> PostgreSQL | `/api/v1/report-formats`, `/api/v1/report-formats/{report_format_id}`, and direct `/api/v1/report-formats/{report_format_id}/export` metadata JSON | `/reportformats` and report-format detail reads now use typed JSON through the authenticated `gsad` proxy. Direct scriptable report-format metadata export reuses the same detail JSON. The native payload keeps trust state, alert/report-config backlinks, and parameter metadata while report-format writes, file import/export, verification, and delete remain inherited until native write semantics are designed. |
| Trashcan counts summary | GSA/GMP `get_trash_*` commands -> gsad -> gvmd XML -> PostgreSQL for contents counts | `/api/v1/trashcan/summary` | `/trashcan` Contents now prefers a counts-only native summary through the authenticated `gsad` proxy. Row-level Trashcan tables and restore/delete/empty mutations remain inherited. The native endpoint reads only base trash-table counts plus hidden tasks and excludes credential/target/scanner row data, child trash tables, `results_trash`, and all secret-adjacent payloads. |
| Operating-system asset reads and metadata export | GSA/GMP OS asset list/detail commands -> gsad -> gvmd XML -> PostgreSQL | `/api/v1/operating-systems`, `/api/v1/operating-systems/{os_id}`, and direct `/api/v1/operating-systems/{os_id}/export` metadata JSON | `/operatingsystems` reads its list through typed JSON via the authenticated `gsad` proxy. Operating-system detail Information and active User Tags now read native JSON. Direct scriptable operating-system metadata export reuses the same detail JSON while inherited paths still own delete, legacy export, tag writes, and retained actions. The native detail uses `oss.uuid` and gvmd asset tables, and it intentionally excludes scanner-control, feed-control, and write semantics. |
| Host asset reads and metadata export | GSA/GMP host asset list/detail commands -> gsad -> gvmd XML -> PostgreSQL | `/api/v1/hosts`, `/api/v1/hosts/{host_id}`, and direct `/api/v1/hosts/{host_id}/export` metadata JSON | `/hosts` reads its list through typed JSON via the authenticated `gsad` proxy. Host detail Information fields and active User Tags now read native JSON. Direct scriptable host metadata export reuses the same detail JSON while inherited paths still own writes, delete identifier, XML export, target creation from host, and tag writes/actions. The native detail uses `hosts.uuid`, safe `host_identifiers`, `host_oss`/`oss`, latest whitelisted `host_details` names (`best_os_cpe`, `best_os_txt`, `traceroute`), and active direct host tags. It intentionally excludes credential/privacy-sensitive identifiers, raw `report_host_details` expansion, report/result/port/application history, and GMP-only `details=1` parity. |
| TLS certificate asset reads and metadata export | GSA/GMP TLS certificate list/detail commands -> gsad -> gvmd XML -> PostgreSQL | `/api/v1/tls-certificates`, `/api/v1/tls-certificates/{certificate_id}`, and direct `/api/v1/tls-certificates/{certificate_id}/export` metadata JSON | `/tlscertificates` reads its list through typed JSON via the authenticated `gsad` proxy. TLS Certificate detail Information fields, active User Tags, validity/trust status, and source provenance now read native JSON. Direct scriptable TLS certificate metadata export reuses the same detail JSON while inherited GMP context still owns certificate download bytes, legacy export, delete, tag writes/actions, and retained actions. The native detail uses `tls_certificates.uuid` plus gvmd source/location/origin/tag tables, and it intentionally excludes stored certificate bytes, scanner-control, feed-control, and write semantics. |
| Scope-report Results | GSA Results tab now uses typed native JSON through the authenticated `gsad` proxy; the inherited gvmd source-report-constrained GMP collection remains available during transition | `/api/v1/scopes/{scope_id}/reports/{scope_report_id}/results` | Browser smoke proves the native Results tab and raw evidence links while GMP compatibility remains intact. |
| Scope-report metrics | `runtime-scope-report-metrics` and the GSA Metrics tab now use the native API; the inherited scope-report metrics GMP command remains available during transition | `/api/v1/scopes/{scope_id}/reports/{scope_report_id}/metrics` | Runtime and browser smoke continue to prove the native path while GMP compatibility remains intact. |
| Scope-report evidence tabs | GSA Results, Hosts, Ports, Applications, Operating Systems, CVEs, TLS Certificates, and Error Messages tabs now use typed native JSON through the authenticated `gsad` proxy | `/api/v1/scopes/{scope_id}/reports/{scope_report_id}/results`, `/hosts`, `/ports`, `/applications`, `/operating-systems`, `/cves`, `/tls-certificates`, and `/errors` | Browser smoke proves aggregated tabs load through native JSON and no longer render per-source raw-report sections. |
| Scope-report retention preview | B-143 dry-run retention planning can be inspected without running destructive retention | `/api/v1/scopes/{scope_id}/reports/{scope_report_id}/retention-plan` | The authenticated `gsad` proxy and direct native API expose the non-destructive preview only. Retention mutation and compaction remain closed until separately designed and validated. |
| Runtime report/scope helpers | Some `turbovasctl` helpers still use inherited control paths; raw report summary/export and scope-report summary/metrics no longer use legacy XML helper scripts | Native API-backed helper calls | `runtime-report-summary`, `runtime-report-export`, `runtime-report-metrics`, `runtime-scope-report-metrics`, and `runtime-scope-report-summary` now use the internal native API. Native result rows carry hostname, NVT family, and description excerpts, and the old `tools/runtime_report.py` XML helper has been removed. Raw report `vulnerability_count` mirrors inherited raw-report summary semantics. |
| Read-only report/scope/target/task/config/tag/override/alert automation | Imported GMP scripts, plus TurboVAS scope/report list scripts | `tools/turbovasctl native-api-request --json --path '/api/v1/...'` or `just native-api-request --json --path '/api/v1/...'` | Raw report, scope, scope-report, target, task, scan-config metadata, tag metadata, override metadata, report-config, redacted alert metadata, CERT-Bund report generation, and scope-report result listing now have a DB-backed native GET path or native helper. The obsolete read-only `gvm-tools` scripts are removed where equivalent native reads exist; alert method/event/condition payloads remain intentionally excluded from the native metadata contract. |
| gvm-tools write/control scripts | Imported GMP scripts for generation or scanner/control workflows | Future native write/control APIs after safety design | Write/control scripts remain compatibility-only until the corresponding native APIs are explicitly designed and proven. Narrow direct write-control slices already cover selected scope, tag, alert metadata, scan-config, saved-filter, credential name/comment metadata, and target name/comment metadata writes, but broad scanner/feed/credential-secret/account/target-collection control remains inherited. |
| Direct scriptable operator API | Temporary automation still uses inherited GMP helpers, `turbovasctl` wrappers, or internal native probes depending on workflow coverage. | Authenticated `/api/v1` access usable by `curl`, generated OpenAPI clients, and TurboVAS-owned automation without GSA, `gsad`, GMP/XML, `python-gvm`, or `gvm-tools` as required interfaces. | First read-only bearer-auth direct development listener lands; next migrate read-only automation to `native-api-request --direct`, add production TLS/host-binding/audit hardening, and add write/control endpoints only after safety review. |

## Expansion Rule

Every native API expansion must state which inherited path it replaces, which
tests prove parity, and what removal criterion it advances. If an endpoint only
forwards XML or creates another untyped payload shape, it does not count as
progress toward this map.
