<!-- SPDX-FileCopyrightText: 2026 Robert Pelfrey <robert@pelfrey.de> -->
<!-- SPDX-License-Identifier: GPL-3.0-or-later -->

# GMP/XML Strangler Map

YAFVS inherits GMP/XML as the main manager protocol. GMP remains useful for
compatibility and high-consequence scanner control, but it should no longer be
the default shape for new YAFVS product data flow. Native HTTP/JSON product
APIs should replace inherited client/tooling paths workflow by workflow.

The legacy-client end-state is complete: `python-gvm` and `gvm-tools` are
removed from product, runtime-helper, test, build, and operator tooling paths.
Remaining GMP/XML work is tracked by the native API owner-tail inventory.

The end-state also includes scriptable access to YAFVS through typed
HTTP/JSON and OpenAPI clients. The authenticated same-origin `gsad` proxy is a
browser migration bridge, not the final API boundary and not a new reason to
keep `gsad` in every automation path. Direct scriptable API work is active
through an opt-in bearer-authenticated
development listener. It exposes a positive allowlist of native reads and,
only with verified operator identity plus the explicit write-control gate,
reviewed write/control operations. Request bounds, admission pressure, request
correlation, and outcome audit logging are implemented. Production TLS,
non-loopback deployment, per-operator rate policy, and production authorization
hardening remain separate from this development boundary.

The machine-readable operation profiles in `api/openapi/yafvs-v1.yaml` are the
current exposure and migration inventory. The generated
`docs/NATIVE_API_OPERATION_REGISTRY.md` records every method/path, surface,
principal, authentication mechanism, write gate, compatibility requirement,
request limit, destructive/confirmation/idempotency posture, audit event, and
maturity state. The contract gate rejects incomplete metadata or generated-doc
drift; independent Rust and C positive allowlists remain fail-closed enforcement
boundaries and are checked against the contract rather than generated into a
broader runtime surface.

The first live proof is the Rust `yafvs-api` sidecar for
raw report reads, scope-report collections, target/task reads, scanner metadata,
saved filters, tags, scan configs, overrides, port lists, schedules, report formats, and report metrics, currently raw report list/detail/result rows/hosts,
target/task internal reads, the browser-backed target and task lists, top-level
Results, Vulnerabilities, Operating Systems, Hosts, TLS Certificates, and
Scanners, Scan Configs, Filters, Tags, Overrides, Port Lists, and Schedules lists, scope-report list, Results, Hosts, Ports, Applications,
Operating Systems, CVEs, TLS Certificates, Error Messages, scope-report Metrics,
and raw report Metrics. It queries PostgreSQL
directly. It is internal by default and has an opt-in bearer-auth direct
development listener for contract-classified reads and explicitly gated
write/control operations. Browser migration now covers the raw `/reports` list,
the `/targets` list, the `/tasks` list, raw-report Results/Hosts, raw-report
and scope-report Metrics, plus all current scope-report evidence tabs through
the authenticated same-origin `gsad` proxy defined in
`docs/NATIVE_API_AUTH_BOUNDARY.md`.

## Workflow Retirement Classes

| Class | Meaning | Current candidates |
| --- | --- | --- |
| Ready for native API proof | Low-risk read workflows with DB-owned state and existing validation. | Raw report reads, scope metadata reads, and remaining helper/tooling replacement paths. |
| Later after product semantics | Workflows needing stronger operator-model decisions first. | Exposure-duration views, owner/patchability/support status, non-operator delivery, BYO inventory. |
| High-consequence control path | Keep an inherited owner only where no separately characterized and reviewed native slice exists. | The generated operation registry is authoritative for current native task, scanner, account/authentication, credential, import, and destructive operations; each operation's residual inherited owner is recorded there. Unlisted behavior remains inherited or unavailable. |
| Compatibility-only | Retain only until no required YAFVS workflow depends on it. | Residual GSA/gsad/gvmd GMP owner tails and direct XML compatibility probes. |

## Initial Map

| Workflow | Current path | Native target | Retirement criterion |
| --- | --- | --- | --- |
| Raw result/report list/detail/evidence tabs | GSA GMP commands -> gsad -> gvmd XML -> PostgreSQL | `/api/v1/results`, `/api/v1/results/{result_id}`, `/api/v1/reports`, `/api/v1/reports/{report_id}`, `/api/v1/reports/{report_id}/results`, `/hosts`, `/ports`, `/applications`, `/operating-systems`, `/cves`, `/tls-certificates`, and `/errors` | `/results` and `/reports` now read their lists through typed JSON. Raw result detail tries `/api/v1/results/{result_id}` first for bounded metadata, result/NVT explanatory fields, NVT-level EPSS context, active result user tags, result CVE/CERT/xref references, and effective active override display metadata. Result-tag assignment uses the separate native tag contract; inherited GMP/XML remains only for exports, actions, and still-unmigrated rich detail surfaces. Override creation and mutation use the typed native override lifecycle. Raw detail summary plus raw report Results, Hosts, Ports, Applications, Operating Systems, CVEs, TLS Certificates, and Error Messages tab reads use native JSON through the authenticated `gsad` proxy. The inherited Closed CVEs report view was removed instead of migrated. |
| Security Information CVE catalog | GSA GMP commands -> gsad -> gvmd XML -> SCAP/PostgreSQL | `/api/v1/cves` and `/api/v1/cves/{cve_id}` | The GSA `/cves` list and `/cve/{id}` detail now read the retained CVE intelligence catalog through native JSON, including EPSS score/percentile, generic URL/tag references, SCAP configuration nodes, and CERT/NVT references where PostgreSQL stores them. EPSS provenance remains out of scope until SCAP/feed schema records a real source. This is intentionally separate from observed report/scope CVEs and `/vulnerabilities`. |
| Observed vulnerability list, detail, and metadata export | Native Rust/PostgreSQL reads; retired `GET_VULNS` XML chain | `/api/v1/vulnerabilities`, `/api/v1/vulnerabilities/{vulnerability_id}`, and `/api/v1/vulnerabilities/{vulnerability_id}/export` | GSA list, detail, single export, selected export, and filtered export use typed native JSON. The obsolete GSA response parser and capability, gsad forwarder/dispatch/validator entry, gvmd parser/formatter and dedicated iterator accessors, and command inventory entry are removed. Generic gsad bulk export rejects vulnerability requests before XML command synthesis. Shared vulnerability filter/aggregate SQL remains because retained aggregate reporting still uses it. The inherited schema changelog remains historical provenance; it does not define a live command. |
| Security Information CPE catalog | GSA GMP commands -> gsad -> gvmd XML -> SCAP/PostgreSQL | `/api/v1/cpes` and `/api/v1/cpes/{cpe_id}` | The GSA `/cpes` list and `/cpe/{id}` detail now read the retained CPE intelligence catalog through native JSON. This is intentionally separate from observed report/scope application and operating-system evidence. |
| Security Information NVT catalog and config-aware detail | Native Rust/PostgreSQL NVT and scan-config reads; separately classified inherited actions remain | `/api/v1/nvts`, `/api/v1/nvts/{nvt_id}`, and `/api/v1/scan-configs/{scan_config_id}` | The GSA `/nvts` list, NVT detail, metadata export, and config-aware NVT preference/timeout view use typed native JSON. The config-aware view composes one native NVT with the selected native scan-config payload; its dead `get_config_nvt` and `save_config_nvt` GSA/gsad XML adapters are removed because typed scan-config preference writes already own the browser mutation. NVT tag assignment uses the separate native tag contract; feed control, scanner control, and separately classified non-tag raw GMP behavior remain inherited. |
| Feed inventory/status read | Native runtime-feed metadata and lock-state read | `/api/v1/feeds` | Feed status pages and notifications use the bounded native runtime-feed inventory. The obsolete GSA capability gate, gsad formatter/forwarder, gvmd `get_feeds` parser/formatter, and GMP schema command are removed; feed generation, synchronization, import, activation, and ownership remain separate feed/intelligence responsibilities. |
| Optional feature inventory | GSA locally disables inherited optional product features that YAFVS does not retain | No network endpoint | The live browser command returns an empty feature set without issuing a request. The dead gsad forwarder/validator, gvmd parser/handler, and GMP schema command for `get_features` are removed. Manager-internal compile-time and runtime feature flags remain separate implementation controls; their existence does not create a client protocol or product capability. |
| Operator capabilities and session liveness | Trusted-team capability model plus authenticated same-origin session endpoints | `GET /api/v1/session/ping` and `POST /api/v1/session/renew` | GSA resolves the complete trusted-team capability set locally and checks session liveness through typed native JSON. The obsolete public gsad `get_capabilities` and `ping` handlers, declarations, dispatch rows, and validator tokens are removed. Manager-backed login still performs its independently classified authenticated `HELP` exchange until the native authentication boundary replaces it. |
| Security Information CERT-Bund and DFN-CERT advisory catalog metadata | GSA GMP list commands -> gsad -> gvmd XML -> CERT PostgreSQL tables; rich details still use feed XML/XSLT | `/api/v1/cert-bund-advisories`, `/api/v1/cert-bund-advisories/{advisory_id}`, `/api/v1/dfn-cert-advisories`, and `/api/v1/dfn-cert-advisories/{advisory_id}` | The GSA advisory lists already read native JSON through the authenticated `gsad` proxy. The new detail endpoints are internal catalog-detail metadata for automation and smoke coverage only; retained rich GSA detail/export behavior remains inherited because PostgreSQL does not store XML-only fields such as CERT-Bund revision history, platform/risk/source links, or DFN extra links. |
| CERT-Bund report helper | Legacy `components/gvm-tools/scripts/certbund-report.gmp.py` parsed GMP/XML report data and used a script-private CSV helper. | `tools/yafvsctl runtime-certbund-report` / `just runtime-certbund-report` backed by native report result/host and CERT-Bund advisory reads. | The native helper preserves task/report selection plus JSON/CSV output for the runtime/operator workflow, so the inherited GMP script and private `ssv_csv.py` helper are removed. This deliberately drops arbitrary `gvm-script`/GMP remote compatibility for that helper; inherited report-format exports remain separate. |
| Raw report metrics | `runtime-report-metrics` and the GSA Metrics tab now use the native API; the inherited `get_report_metrics` GMP command remains available during transition | `/api/v1/reports/{report_id}/metrics` | Runtime and browser smoke continue to prove the native path while GMP compatibility remains intact. |
| Scope list/detail and metadata/membership writes | GSA scope pages; retired browser-side and server-side GMP scope mutation commands | `/api/v1/scopes` and `/api/v1/scopes/{scope_id}` | Scope metadata, membership reads, create, update, and delete use typed JSON through the authenticated browser proxy. Direct bearer-token writes additionally require direct write-control and a verified operator. The GSA adapter, gsad dispatch, and gvmd parser/SQL layer no longer expose legacy scope mutation commands; scope reads, reports, and organization-scope maintenance remain intact. |
| Scope-report list/detail | GMP scope-report commands and GSA scope-report pages | `/api/v1/scope-reports` and canonical scoped detail path | GSA list/detail reads use server-backed JSON collections and browser smoke remains green. |
| Target reads and bounded write-control | GSA/GMP target commands -> gsad -> gvmd XML -> PostgreSQL | `/api/v1/targets`, `/api/v1/targets/{target_id}`, direct target create/patch/clone/trash/restore/hard-delete, and `/api/v1/targets/{target_id}/export` when write-control is enabled for mutations. | `/targets`, target detail Information, metadata export, simple manual target create, bounded scan-input patching, credential-link references, native clone, trash, restore, and hard-delete now use typed JSON through the authenticated `gsad` proxy or direct write-control. The task-target replacement contract clones the old target with its settings, credential links, and tags before rebinding the task; it trashes the old target only when unreferenced. File/host-filter input variants and credential secret material remain inherited. Secret credential material is never exposed. |
| Task reads, writes, metadata export, clone, restore, permanent trash deletion, start, stop, and target replacement | GSA/GMP task commands -> gsad -> gvmd XML -> PostgreSQL; retained paths now use native task contracts | `/api/v1/tasks`, `/api/v1/tasks/{task_id}`, direct `/api/v1/tasks/{task_id}/export`, `POST /api/v1/tasks/{task_id}/clone`, `POST /api/v1/tasks/{task_id}/restore`, `DELETE /api/v1/tasks/{task_id}/trash`, `POST /api/v1/tasks/{task_id}/start`, `POST /api/v1/tasks/{task_id}/stop`, and `POST /api/v1/tasks/{task_id}/replace-target` | `/tasks` and task detail Information reads, create, metadata patch, authoritative clone, safe live-to-trash, restore, permanent trash deletion, guarded start, guarded stop, and strict New/no-report target replacement use typed JSON through the authenticated `gsad` proxy or direct write-control. Restore moves preserved results and task/report/result tag locations back to live state, remaps result-tag row identities through stable result UUIDs, invalidates report counts, rejects non-live dependencies, and does not revive retired row-level permission mutations. Permanent deletion is trash-only, blocks active task/report states and scope-snapshot report references, and removes report evidence plus task metadata in one locked transaction. Clone uses gvmd's operator ownership and transaction path to preserve preferences/defaults, alerts, schedule state, and New-task state. It does not directly start a scan, but a copied schedule can start the clone when due. Replacement takes explicit hosts only, atomically clones and rebinds without starting a scan, and the native tooling command replaces the inherited GMP script. Task create includes transactional optional schedule/alert links plus host-order preference forwarding through OSP/OpenVAS and OpenVASD; the native CSV helper replaces the inherited bulk creator after full reference preflight. Start creates the report plus gvmd `scan_queue` request. Stop uses a private bounded shared-secret Unix command with gvmd-owned ACL/scanner/report/queue semantics. Native CSV start/stop and stop-all orchestration also replace their inherited scripts. File export, resume, and other scanner-control semantics remain inherited. |
| Scanner reads, retained lifecycle writes, bounded verification, and metadata export | Native Rust/PostgreSQL lifecycle; retained manager paths only for explicitly listed compatibility behavior | `/api/v1/scanners`, `/api/v1/scanners/{scanner_id}`, clone/restore/trash endpoints, bounded verify, retained configuration replacement, and direct metadata export | Scanner list/detail, create, metadata and full-configuration replacement, clone, live-to-trash, restore, hard-delete, bounded local verification, and native JSON metadata export use typed JSON through the authenticated `gsad` proxy or direct write-control. Native create and full replacement own validated relay configuration; clone preserves inherited behavior by omitting relay settings, while trash/restore round-trip them. Task and tag references are location-aware and atomically rebound. Browser and GMP/XML scanner reads, exports, mutations, and verification plus the duplicate gvmd CLI scanner listing and mutation commands are removed. Retained manager CLI compatibility remains only for remote TLS/relay verification while native ownership is intentionally local and bounded; external relay-file synchronization, legacy file export/download formats, credential/certificate download context, trash accounting internals, and deeper scanner-control behavior remain inherited. |
| Scan-config lifecycle and typed preference writes | Native Rust/PostgreSQL reads and guarded writes; retained raw GMP behavior is separately classified | `/api/v1/scan-configs`, `/api/v1/scan-configs/{scan_config_id}`, import/clone/restore/trash endpoints, and typed family/NVT preference writes | `/scan-configs`, detail metadata, active User Tags, task backlinks, import, clone, metadata and typed scanner/NVT preference updates, family NVT selection, restore, trash, and hard-delete use native JSON and guarded ownership. The payload retains family/NVT counts, growth flags, predefined/deprecated state, in-use state, owner, comments, timestamps, and configured preference context. Legacy export/download formats and separately classified raw GMP behavior remain inherited. |
| Saved filter reads and write-control | Native GSA/Rust filter adapter; dedicated `GET_FILTERS` XML transport and browser/server GMP mutations retired | `/api/v1/filters`, `/api/v1/filters/{filter_id}`, clone/restore/trash endpoints, and `/api/v1/filters/{filter_id}/export` | `/filters`, filter detail, metadata create/update, clone, trash, restore, hard-delete, single and bulk metadata export, and native bulk deletion use typed JSON through the authenticated `gsad` proxy or direct write-control when enabled. Native reads expose the real human-owned mutability boundary. Bulk deletion preflights the complete candidate set before its first request; the subsequent guarded deletes are sequential rather than atomic and report committed progress if a later delete fails. The obsolete GSA XML parsers, gsad forwarders/dispatch/validator entries, gvmd parser/formatter and command-only iterator helpers, and live GMP schema definition are removed. Generic gsad bulk export rejects filters before XML synthesis. The `get_filters` name remains as UI/manager authorization vocabulary, while shared applied-filter parsing, report filtering, alert linkage, resource-name lookup, and generic aggregate compatibility remain separately retained. Filter terms remain authenticated operator data, not public catalog data. |
| Tag metadata, assigned-resource, and resource-name reads | Native GSA/Rust tag adapter; dedicated `GET_TAGS` XML transport retired | `/api/v1/tags`, `/api/v1/tags/{tag_id}`, `/api/v1/tags/{tag_id}/resources`, and `/api/v1/tags/resource-names/{resource_type}` | `/tags`, tag detail metadata, the Assigned Items tab, and the Tag dialog resource-name picker use typed JSON through the authenticated `gsad` proxy for supported resource types, including redacted Alert id/name lookup. Assigned resources and resource names are strict-whitelist references with only id/type/name for existing detail links or picker labels. The obsolete gsad aliases, gvmd parser/formatter and command-only iterator helpers, and live GMP schema definition are removed. `get_tags` remains native authorization and UI cache vocabulary; generic tag aggregates and shared resource-tag expansion in still-retained responses remain separately owned behavior. |
| Direct tag lifecycle and active-resource writes | Native GSA/Rust tag adapter and direct Rust/PostgreSQL transactions; raw tag mutation and generic restore transports retired | Direct `POST /api/v1/tags`, `PATCH /api/v1/tags/{tag_id}`, clone/restore/trash/hard-delete endpoints, and `POST /api/v1/tags/{tag_id}/resources` when direct write-control is enabled. | Browser and bearer-token tag creation, metadata updates, enable/disable, clone, trash, restore, hard-delete, explicit add/remove/set assignment, and typed all-filtered assignment use native write-control with verified operator identity. Each typed selection reuses the supported native collection predicate, requires the operator-confirmed match count, caps the selection, checks every owner-bearing resource, and mutates in one transaction. The raw gvmd/GMP `CREATE_TAG`, `MODIFY_TAG`, `DELETE_TAG`, and generic `RESTORE` parser/schema/help surfaces and duplicate SQL writers are removed because all supported restore lifecycles are native. The prior browser/gsad mutation handlers, private tag control commands, task-create follow-up mutation, dynamic bulk-delete synthesis, and unbounded raw `resource_filter` path remain absent. Unsupported resource types and unbounded raw filter expressions fail closed rather than falling back to the legacy manager. Native authorization names, generic tag aggregates, shared resource-tag expansion, tag tables, trash accounting, and resource cleanup remain independently owned behavior. |
| Credential reads, bounded native creation and clone, restore, and trash hard-delete | GSA/GMP credential list/detail commands -> gsad -> gvmd XML -> PostgreSQL; credential secret/control writes remain partly inherited. | `/api/v1/credentials`, `/api/v1/credentials/{credential_id}`, `POST /api/v1/credentials`, direct `PATCH /api/v1/credentials/{credential_id}`, `POST /api/v1/credentials/{credential_id}/clone`, `POST /api/v1/credentials/{credential_id}/restore`, and `DELETE /api/v1/credentials/{credential_id}/trash` when write-control is enabled. | Native reads expose redacted credential metadata. Native creation supports username/password and username/SSH-key credentials with write-only secret inputs. Native clone copies metadata, opaque secret rows, and active tag links transactionally without loading or returning secret values; the new credential is owned by the requesting operator. The GSA/gsad clone bridge, gvmd copy implementation, and credential-copy GMP schema are retired; a parser tombstone explicitly rejects legacy `<copy>` requests so they cannot degrade into ordinary credential creation. Native restore copies opaque secret rows within PostgreSQL without loading or returning them, preserves `allow_insecure`, restores trash-side target/scanner references and tag locations, rejects collisions and ownerless rows, and returns only redacted metadata. Native hard-delete accepts only a human-owned trash row, fails closed on trash target/scanner/alert-delivery references, removes tag links, and destroys opaque secret rows entirely inside PostgreSQL without selecting or returning values. The broken EXE, RPM, and DEB credential-installer downloads, their subprocess generators, package-setting state, icons, bridge handling, and GMP schema alternatives are removed rather than reproduced. Public-key and client-certificate downloads remain inherited pending native ownership. Direct bearer-token PATCH can update name/comment metadata only with verified operator identity and `YAFVS_API_DIRECT_WRITE_CONTROL`; broader secret updates, other credential types, type/allow-insecure mutation, live scanner/target link mutation, moving live credentials to trash, live deletion, and secret-bearing updates remain inherited. The unused external Credential Store/CyberArk surface is removed. |
| Override reads and write-control | GSA native override adapter; retired browser-side and server-side GMP override mutations | `/api/v1/overrides`, `/api/v1/overrides/{override_id}`, metadata export, clone, restore, and trash endpoints | `/overrides`, override detail, create, metadata patch, clone, trash, restore, hard-delete, single metadata export, and browser bulk JSON download use typed JSON through the authenticated `gsad` proxy or direct write-control when enabled. The GSA capability surface, gsad dispatch, gvmd parser/schema, and duplicate gvmd SQL writer no longer expose legacy create/copy/modify/delete override commands. Override evaluation, report-cache behavior, reads, result-specific expansion, trash-empty accounting, user cleanup, and retained legacy read/export plumbing remain intact. |
| Port-list reads and write-control | GSA native port-list adapter; retired browser-side and server-side GMP port-list mutations | `/api/v1/port-lists`, `/api/v1/port-lists/{port_list_id}`, `/api/v1/port-list-imports`, clone/restore/trash endpoints, and native range-set write endpoints | `/port-lists`, port-list detail, range add/delete, typed create, metadata/range update, clone, trash, restore, hard-delete, exported-XML import, and browser bulk JSON download use typed JSON through the authenticated `gsad` proxy or direct write-control when enabled. The GSA capability surface, gsad dispatch, gvmd parser/schema, and duplicate gvmd SQL writer no longer expose legacy port-list or port-range mutations. Native import accepts one exported XML payload with explicit TCP/UDP ranges and preserves the imported UUID. Feed/predefined import, target inline port-range creation, reads, restore/trash accounting, and retained generic bulk export remain intact. |
| Schedule reads and bounded write-control | GSA/GMP schedule list/detail/write/export commands -> gsad -> gvmd XML -> PostgreSQL | `/api/v1/schedules`, `/api/v1/schedules/{schedule_id}`, clone/restore/trash endpoints, and `/api/v1/schedules/{schedule_id}/export` | `/schedules`, schedule detail, metadata patch, clone, trash, restore, hard-delete, and JSON metadata export now use typed JSON through the authenticated `gsad` proxy or direct write-control when enabled. The native payload keeps iCalendar recurrence data and task backlinks while schedule create, calendar edit, and task next-time recalculation remain inherited until native iCalendar semantics are designed. |
| Report-format reads and metadata export | Native GSA/Rust report-format adapter; dedicated `GET_REPORT_FORMATS` XML transport retired | `/api/v1/report-formats`, `/api/v1/report-formats/{report_format_id}`, and direct `/api/v1/report-formats/{report_format_id}/export` metadata JSON | `/reportformats` and report-format detail pages read typed JSON through the authenticated `gsad` proxy. Native command and API adapters separately provide single and bulk JSON metadata export. The obsolete GSA XML-capable collection inheritance, gsad read/export aliases, gvmd parser/formatter, and live GMP schema command are removed; generic gsad XML bulk export rejects report formats case-insensitively before command synthesis. YAFVS deliberately retires custom executable report-format create/import/edit/verify/clone/delete. Retained trusted built-in/feed import and application, alert references, report generation, shared manager iterators, and the `get_report_formats` authorization vocabulary remain independently owned behavior. |
| Trashcan inventory and lifecycle | Native Rust/PostgreSQL inventory, preview, empty, and eleven complete typed resource lifecycles | `/api/v1/trashcan/summary`, `/api/v1/trashcan/items`, `/api/v1/trashcan/empty-preview`, `/api/v1/trashcan/empty`, and native restore/hard-delete owners for alert, credential, filter, override, port list, scan config, scanner, schedule, tag, target, and task | `/trashcan` contents, summary, confirmed emptying, and eleven complete resource lifecycles use typed native JSON through the authenticated `gsad` proxy. The GSA inventory no longer falls back to twelve independent `get_trash_*` requests and fails closed when native HTTP is unavailable; the twelve superseded gsad aliases, dispatch rows, declarations, and validator tokens are deleted. Task restore preserves reports and remaps result-tag identities; task permanent deletion is trash-only, blocks active work and scope-snapshot sources, and removes its report evidence transactionally. Credential secret rows remain opaque during restore and are deleted without being selected or returned during hard-delete. The generic GSA/gsad GMP restore and permanent-delete compatibility bridges are removed. Individual report-format restore and permanent delete remain absent because restoration could resurrect the retired custom executable-format surface; confirmed owner-scoped Trashcan emptying owns legacy row/directory cleanup. Native inventory excludes child-table and secret-adjacent payloads. Separately classified raw resource `GET_*` requests with `trash="1"` remain inherited except filters, report formats, and tags, whose dedicated reads are retired. |
| Timezone inventory | Native Rust/PostgreSQL read used by the browser schedule workflow | `/api/v1/timezones` | The browser reads the ordered PostgreSQL timezone catalog through typed JSON. The dead GSA command marker and the complete gsad/GMP/gvmd `get_timezones` compatibility chain are removed; schedule validation and calendar mutation remain independently characterized manager semantics until their ownership migrates. |
| Global NVT-family inventory | Native Rust/PostgreSQL read used by the browser scan-config workflow | `/api/v1/nvt-families` | The browser reads the ascending feed-family catalog and per-family NVT counts through typed JSON. The native query preserves the inherited exclusion of the internal `Credentials` family. The complete GSA command marker and gsad/GMP/gvmd `get_nvt_families` compatibility chain are removed; per-config family selection and NVT preference semantics remain separately owned native contracts. |
| Legacy appliance licensing | Inherited GSA/gsad/GMP/gvmd license read and update backed by optional libtheia | Deleted; no native replacement | YAFVS does not use Greenbone appliance or subscription licensing. The unmounted browser provider/notification, `get_license`/`save_license` adapters, gvmd `get_license`/`modify_license` parser and handlers, libtheia manager code, schema commands, and optional build dependency are removed end to end. Source-code licensing, feed terms, attribution, and provenance remain separate governance responsibilities and are unaffected. |
| Operating-system asset reads and metadata export | GSA/GMP OS asset list/detail commands -> gsad -> gvmd XML -> PostgreSQL | `/api/v1/operating-systems`, `/api/v1/operating-systems/{os_id}`, and direct `/api/v1/operating-systems/{os_id}/export` metadata JSON | `/operatingsystems` reads its list through typed JSON via the authenticated `gsad` proxy. Operating-system detail Information and active User Tags now read native JSON. Direct scriptable operating-system metadata export reuses the same detail JSON. Tag assignment uses the separate native tag contract; inherited paths still own operating-system delete, legacy export, and retained actions. The native detail uses `oss.uuid` and gvmd asset tables, and it intentionally excludes scanner-control, feed-control, and other write semantics. |
| Host asset reads and metadata export | GSA/GMP host asset list/detail commands -> gsad -> gvmd XML -> PostgreSQL | `/api/v1/hosts`, `/api/v1/hosts/{host_id}`, and direct `/api/v1/hosts/{host_id}/export` metadata JSON | `/hosts` reads its list through typed JSON via the authenticated `gsad` proxy. Host detail Information fields and active User Tags now read native JSON. Direct scriptable host metadata export reuses the same detail JSON. Tag assignment uses the separate native tag contract; inherited paths still own other host writes, XML export, and target creation from a host. The native detail uses `hosts.uuid`, safe `host_identifiers`, `host_oss`/`oss`, latest whitelisted `host_details` names (`best_os_cpe`, `best_os_txt`, `traceroute`), and active direct host tags. It intentionally excludes credential/privacy-sensitive identifiers, raw `report_host_details` expansion, report/result/port/application history, and GMP-only `details=1` parity. |
| TLS certificate asset lifecycle | Native Rust/PostgreSQL reads and native browser deletion | `/api/v1/tls-certificates`, `/api/v1/tls-certificates/{certificate_id}`, direct metadata export, PEM JSON, and native delete | The dedicated GSA/gsad/GMP TLS read/create/modify/delete transport and generic XML bulk export/delete synthesis are removed. Browser list/detail, deletion, PEM download, metadata export, and active User Tags are native-only and fail closed without the native API. TLS scan/report ingestion, source provenance, user cleanup, inheritance, writable/in-use checks, and database tables remain independently owned. Generic aggregate compatibility and report TLS evidence remain separately retained. |
| Scope-report Results | GSA Results tab now uses typed native JSON through the authenticated `gsad` proxy; the inherited gvmd source-report-constrained GMP collection remains available during transition | `/api/v1/scopes/{scope_id}/reports/{scope_report_id}/results` | Browser smoke proves the native Results tab and raw evidence links while GMP compatibility remains intact. |
| Scope metadata reads | Native scope list/detail reads; obsolete public gsad aliases and authenticated gvmd `GET_SCOPE`/`GET_SCOPES` XML transport retired | `/api/v1/scopes`, `/api/v1/scopes/{scope_id}` | Native `fetchNativeScopes` / `fetchNativeScope` callsites remain; scope lifecycle, authorization, and scope-report behavior are retained. |
| Scope-report metrics | `runtime-scope-report-metrics` and the GSA Metrics tab now use the native API; the inherited scope-report metrics GMP command remains available during transition | `/api/v1/scopes/{scope_id}/reports/{scope_report_id}/metrics` | Runtime and browser smoke continue to prove the native path while GMP compatibility remains intact. |
| Scope-report evidence tabs | GSA Results, Hosts, Ports, Applications, Operating Systems, CVEs, TLS Certificates, and Error Messages tabs now use typed native JSON through the authenticated `gsad` proxy | `/api/v1/scopes/{scope_id}/reports/{scope_report_id}/results`, `/hosts`, `/ports`, `/applications`, `/operating-systems`, `/cves`, `/tls-certificates`, and `/errors` | Browser smoke proves aggregated tabs load through native JSON and no longer render per-source raw-report sections. |
| Scope-report retention preview | B-143 dry-run retention planning can be inspected without running destructive retention | `/api/v1/scopes/{scope_id}/reports/{scope_report_id}/retention-plan` | The authenticated `gsad` proxy and direct native API expose the non-destructive preview only. Retention mutation and compaction remain closed until separately designed and validated. |
| Runtime report/scope helpers | Some `yafvsctl` helpers still use inherited control paths; raw report summary/export and scope-report summary/metrics no longer use legacy XML helper scripts | Native API-backed helper calls | `runtime-report-summary`, `runtime-report-export`, `runtime-report-metrics`, `runtime-scope-report-metrics`, and `runtime-scope-report-summary` now use the internal native API. Native result rows carry hostname, NVT family, and description excerpts, and the old `tools/runtime_report.py` XML helper has been removed. Raw report `vulnerability_count` mirrors inherited raw-report summary semantics. |
| Read-only report/scope/target/task/config/tag/override/alert automation | Imported GMP scripts, plus YAFVS scope/report list scripts | `just native-api-request --json --path '/api/v1/...'` | Raw report, scope, scope-report, target, task, scan-config metadata, tag metadata, override metadata, redacted alert metadata, CERT-Bund report generation, and scope-report result listing now have a DB-backed native GET path or native helper. The obsolete read-only `gvm-tools` scripts are removed where equivalent native reads exist; alert method/event/condition payloads remain intentionally excluded from the native metadata contract. |
| Retired gvm-tools write/control scripts | Imported GMP scripts for generation or scanner/control workflows | Typed native write/control APIs plus bounded `yafvsctl` compositions | All tracked scripts are removed. Retained behavior is native; non-retained compatibility behavior stays deleted. |
| Direct scriptable operator API | Authenticated native reads and guarded writes are exposed through the direct development listener and YAFVS-owned helpers. | Authenticated `/api/v1` access usable by `curl`, generated OpenAPI clients, and YAFVS-owned automation without GSA, `gsad`, GMP/XML, `python-gvm`, or `gvm-tools` as required interfaces. | Continue production TLS/host-binding/audit hardening and remaining workflow-owner replacement under their explicit contracts. |

## Expansion Rule

Every native API expansion must state which inherited path it replaces, which
tests prove parity, and what removal criterion it advances. If an endpoint only
forwards XML or creates another untyped payload shape, it does not count as
progress toward this map.
